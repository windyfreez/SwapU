<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.itsean.swapu_admin.mapper.CategoryMapper">

    <resultMap id="CategoryResultMap" type="com.itsean.swapu_admin.entity.Category">
        <id property="id" column="id"/>
        <result property="name" column="name"/>
        <result property="sort" column="sort"/>
        <result property="status" column="status"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
        <result property="createUser" column="create_user"/>
        <result property="updateUser" column="update_user"/>
    </resultMap>

    <!-- 用户端：只展示启用中的分类 -->
    <select id="list" resultMap="CategoryResultMap">
        select * from category
        where status = 1
        order by sort asc,create_time desc
    </select>

    <!-- 管理端：展示全部分类（含停用），按 sort 升序 -->
    <select id="listAll" resultMap="CategoryResultMap">
        select * from category
        order by sort asc, id asc
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into category (name, sort, status, create_time, update_time, create_user, update_user)
        values (#{name},#{sort},#{status},#{createTime},#{updateTime},#{createUser},#{updateUser})
    </insert>

    <!-- 只更新非 null 字段 -->
    <update id="update">
        update category
        <set>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="sort != null">
                sort = #{sort},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser},
            </if>
        </set>
        where id = #{id}
    </update>

</mapper>
